{
  "name": "@natco/ad-domain-lists",
  "version": "1.0.0",
  "dependencies": {
    "nslookup": "^1.1.1"
  },
  "devDependencies": {
    "@types/node": "^9.4.7",
    "rimraf": "^2.6.2"
  },
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "prebuild": "yarn clean",
    "clean": "rimraf lib",
    "test": "node lib/index.js",
    "pretest": "yarn build"
  }
}
