{
  "name": "@natco/env-setup",
  "private": true,
  "devDependencies": {
    "lerna": "^2.9.0"
  },
  "scripts": {
    "build": "lerna run build",
    "prebuild": "yarn && lerna bootstrap",
    "start": "lerna run start",
    "deploy": "sh scripts/coreos-deploy.sh",
    "add-pkg": "lerna add"
  },
  "workspaces": [
    "containers/*"
  ]
}
