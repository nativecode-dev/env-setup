{
  "author": "opensource@nativecode.com",
  "license": "MIT",
  "name": "pihole",
  "version": "1.0.1-0",
  "scripts": {
    "build": "docker build --rm -t nrepo.io/pihole .",
    "start": "docker run --rm --name pihole nrepo.io/pihole",
    "prestart": "yarn build",
    "poststart": "docker stop pihole"
  }
}
