{
  "author": "opensource@nativecode.com",
  "license": "MIT",
  "name": "appauth",
  "repository": "git@github.com:nativecode-dev/docker-nginx-basic-auth.git",
  "version": "1.0.1-0",
  "scripts": {
    "build": "docker build --rm -t nrepo.io/appauth .",
    "start": "docker run --rm --name appauth nrepo.io/appauth",
    "prestart": "yarn build",
    "poststart": "docker stop appauth"
  }
}
